{{ form_start(form) }}
    {{ form_widget(form) }}
    {{ include('admin/components/_actions.html.twig', {
        'action': action,
        'entity': entity,
        'entityName': 'user'
    }) }}
{{ form_end(form) }}
