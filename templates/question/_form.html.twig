{{ form_start(form) }}
    {{ form_widget(form) }}

    {{ include('components/_actions.html.twig', {
        'action': action,
        'entity': entity | default(null),
        'entityName': 'question'
    }) }}

{{ form_end(form) }}
