<form method="post" action="{{ path('app_examination_sheet_delete', {'id': examination_sheet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ examination_sheet.id) }}">
    <button class="btn">Delete</button>
</form>
