{{ form_start(form) }}
    {{ form_widget(form) }}

    {{ include('components/_actions.html.twig', {
        'action': action,
        'entity': entity,
        'entityName': 'examination_sheet'
    }) }}

{{ form_end(form) }}
