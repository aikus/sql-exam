<form method="post" action="{{ path('app_test_question_delete', {'id': test_question.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ test_question.id) }}">
    <button class="btn">Delete</button>
</form>
