{{ form_start(form) }}
    {{ form_widget(form) }}

    {{ include('components/_actions.html.twig', {
        'action': action,
        'entityId': entityId,
        'entityName': 'exam'
    }) }}

{{ form_end(form) }}
