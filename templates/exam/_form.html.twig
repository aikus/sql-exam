{{ form_start(form) }}
    {{ form_widget(form) }}

    {{ include('components/_actions.html.twig', {
        'action': action,
        'entity': entity | default(null),
        'entityName': 'exam'
    }) }}

{{ form_end(form) }}
